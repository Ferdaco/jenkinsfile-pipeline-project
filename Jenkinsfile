pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Power in Yourself'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}
